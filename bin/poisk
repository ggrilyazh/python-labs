def plus_one(n):
    return n + 1

def minus_one(n):
    return int(str(n - 1))


def find_z(x, y):
    z = 0

    while z + y != x:
        if z + y < x:
            z = plus_one(z)
        else:
            z = minus_one(z)

    return z


print(find_z(2, -1))
