def find_z_binary(x, y):
    left = -1000
    right = 1000

    while left <= right:
        mid = (left + right) // 2

        if mid + y == x:
            return mid

        elif mid + y < x:
            left = mid + 1

        else:
            right = mid - 1


print(find_z_binary(74, 481))
